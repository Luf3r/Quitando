<%= turbo_frame_tag "group_dialog" do %>
  <section aria-labelledby="nova-despesa-dialogo" class="p-5">
    <div class="dialog-header"><h1 id="nova-despesa-dialogo">Nova despesa</h1><button type="button" data-action="group-dialog#close" class="ui-button ui-button--quiet ui-button--compact">Fechar</button></div>
    <%= render "expenses/form", group: @group, dashboard: @dashboard, expense_form: @expense_form %>
  </section>
<% end %>
